@page
@model DocumentGenerator.Models.FormModel
@{}

<h1>Step1</h1>

@using (Html.BeginForm("Step2", "Form", FormMethod.Post))
{
    <div class="form-group">
        <div>
            @Html.HiddenFor(x => x.Name, "Value")
        </div>
        <div>
            @Html.LabelFor(m => m.Step1Model.Field1)
        </div>

        <div>
            @Html.TextBoxFor(model => model.Step1Model.Field1, new { @class = "form-control" })
        </div>
    </div>

    <div class="form-group">
        <div>
            @Html.LabelFor(m => m.Step1Model.Field2)
        </div>

        <div>
            @Html.TextBoxFor(model => model.Step1Model.Field2, new { @class = "form-control" })
        </div>
    </div>

    <div>
        <input type="submit" value="Next" />
    </div>

}
