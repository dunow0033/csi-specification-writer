using DocumentGenerator.Service.Models.Step1;
using FastExcel;

namespace DocumentGenerator.Service.Interfaces.Step1
{
    public interface IGpsDistributor
    {
        // int CreateOrUpdate(int? idValue, string path, GpsDistributor gpsDistributor);
        // GpsDistributor? GetByRow(Row row);
    }
}
